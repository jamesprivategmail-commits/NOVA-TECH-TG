{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "Secret used to sign JWT session tokens. A development placeholder is auto-generated; replace with your own (e.g. `openssl rand -hex 32`) for production.",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    }
  ]
}
