# Repo-level defaults so the app boots before any platform secrets are provided.
# /run/base44/app.env (platform-managed) overrides anything here.
JWT_SECRET=dev-placeholder-replace-in-dashboard
ADMIN_NOVA_IDS=
